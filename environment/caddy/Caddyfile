# Site address is `localhost` for local development. A real deployment sets
# BUEM_SITE_ADDRESS in env/proxy.env to its own domain, or Caddy will
# neither serve it nor be able to provision a certificate for it.

# Caddy defaults to advertising HTTP/3 (Alt-Svc: h3) on every response. The
# compose file only publishes the HTTPS port as TCP, so browsers that cache
# that Alt-Svc header and switch to QUIC on a later request get no response
# at all — this looked like intermittent request failures against a healthy
# backend. h2 over the published TCP port is all that's actually reachable.
{
	servers {
		protocols h1 h2
	}
}

{$BUEM_SITE_ADDRESS:localhost} {
	# Single source of truth for allowed origins is ALLOWED_ORIGINS in
	# env/common.env (also read directly by anything that needs to check origins).
	# ALLOWED_ORIGINS_PATTERN is derived from it at container startup
	# (see docker-compose.yml's entrypoint) into the regex form this
	# matcher needs — nothing here is hardcoded per origin.
	@allowed_origin header_regexp Origin {$ALLOWED_ORIGINS_PATTERN}

	@preflight method OPTIONS

	# Unlike ignis's Caddyfile (which hardcodes this value), the key is read
	# from BUEM_API_KEY at container start — a real key belongs in
	# env/proxy.env (untracked in production, see .env.example), not
	# committed source.
	@authorized header X-Api-Key {$BUEM_API_KEY}

	# `route` keeps these handlers in the order written. Without it Caddy
	# sorts `handle` blocks by path-matcher specificity, which would move
	# /buem/health ahead of the preflight block and change which one
	# answers an OPTIONS /buem/health (see ignis's Caddyfile, which this
	# mirrors).
	route {
		# Preflight (OPTIONS): answered here, never forwarded to the app.
		# X-Api-Key is a proxy-only concept — buem-gateway doesn't know it
		# exists, and it shouldn't have to.
		handle @preflight {
			header @allowed_origin Access-Control-Allow-Origin "{http.regexp.allowed_origin.1}"
			header Access-Control-Allow-Methods "GET, POST, OPTIONS"
			header Access-Control-Allow-Headers "Content-Type, X-Api-Key"
			header Access-Control-Max-Age "86400"
			respond 204
		}

		# Deliberately outside the authorised route: container health checks,
		# orchestrators and uptime monitors must not need the API key. This
		# path is publicly reachable on any deployment, so keep its response
		# to liveness only — no version, config or database detail. Nested
		# under /buem/ (not bare /health) so it doesn't collide with sibling
		# services' own health checks once this sits behind the shared
		# Orchestrator (see BuEM/weather's /v1/weather/health for the same
		# reasoning).
		handle /buem/health {
			reverse_proxy buem-gateway:{$APP_PORT:8080}
		}

		handle @authorized {
			reverse_proxy buem-gateway:{$APP_PORT:8080}
		}

		# Without the CORS header here too, a missing/wrong X-Api-Key shows up
		# in the browser as an opaque CORS failure instead of this 403 —
		# indistinguishable from a real CORS misconfiguration during debugging.
		handle {
			header @allowed_origin Access-Control-Allow-Origin "{http.regexp.allowed_origin.1}"
			respond "Forbidden: missing or invalid API key" 403
		}
	}
}
